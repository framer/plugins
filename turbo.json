{
    "$schema": "https://turbo.build/schema.json",
    "pipeline": {
        "build-remote": {
            "env": ["USE_BASE_PATH"],
            "outputs": ["dist/**"]
        },
        "//#copy": {}
    }
}
