{
    "$schema": "https://turbo.build/schema.json",
    "pipeline": {
        "build": {
            "env": ["USE_BASE_PATH"],
            "outputs": ["dist/**"]
        },
        "//#copy": {}
    }
}
